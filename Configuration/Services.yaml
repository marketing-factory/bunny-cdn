services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: false

  Mfd\BunnyCdn\:
    resource: '../Classes/'
    exclude:
      # Cache backends are constructed directly by TYPO3\CMS\Core\Cache\CacheManager
      # via a plain `new`, never through the DI container.
      - '../Classes/Cache/Backend/*'

  Mfd\BunnyCdn\Service\BunnyCdnService:
    public: true

  Mfd\BunnyCdn\ViewHelpers\IsActiveViewHelper:
    public: true

  Mfd\BunnyCdn\ViewHelpers\IsViaBunnyViewHelper:
    public: true
